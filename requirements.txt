# PyGeoModels MCP 服务依赖

# 数值计算基础库 (pygeoc 依赖)
numpy>=1.20.0

# 科学计算库
scipy>=1.7.0

# 地理计算工具 (需要编译 C 扩展)
pygeoc>=0.2.0

# HTTP 客户端
requests>=2.31.0

# MCP 框架
fastmcp>=3.1.1

# ASGI 服务器
uvicorn[standard]>=0.42.0

# Web 框架
starlette[full]>=0.27.0

# 其他依赖
typing-extensions>=4.0.0
pydantic>=2.0.0

# ---------- 可选依赖 ----------
# GDAL 地理数据处理（可选）
# 仅当启用"地形分析工具集"（local_config.ini 中 enable_terrain_analysis_tools=true）时需要。
# 不安装不影响 MCP 服务的默认启动，以及模型管理(EGC)/AOI 工具的运行。
